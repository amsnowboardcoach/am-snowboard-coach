/** Días por petición (nov→jun ≈ 240 días; una carga suele cubrir toda la temporada) */
export const BOOKING_AVAILABILITY_FETCH_DAYS = 240;

/** Temporada de reserva en calendario: noviembre → junio (ver lib/booking/season.ts) */
export const BOOKING_SEASON_LABEL = "noviembre a junio";
