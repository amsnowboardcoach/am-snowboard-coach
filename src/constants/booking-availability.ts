/** Días por petición a la API (carga por ventanas al cambiar de mes) */
export const BOOKING_AVAILABILITY_FETCH_DAYS = 120;

/** Temporada de reserva en calendario: noviembre → mayo (ver lib/booking/season.ts) */
export const BOOKING_SEASON_LABEL = "noviembre a mayo";
